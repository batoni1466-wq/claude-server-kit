# Never commit anything with real secrets a user fills in from this kit.
config.env
design-secret.txt
design-url.txt
*credential*
*.credentials.json
auth.db
*.tgz
__pycache__/
*.pyc

# Local working copy of the kit: private skills, server addresses, SSH aliases.
# The kit itself never needs them.
.claude/
